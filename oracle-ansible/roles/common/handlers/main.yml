---
- name: Restart chronyd
  service:
    name:  chronyd
    state: restarted

- name: Reload udev rules
  shell: udevadm control --reload-rules && udevadm trigger && udevadm settle
  changed_when: false
